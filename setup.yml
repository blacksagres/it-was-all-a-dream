- name: Dev Machine Setup 🖖
  hosts: localhost
  tasks:
    # --- Debian/Ubuntu ---

    - name: Update apt package index
      ansible.builtin.apt:
        update_cache: yes
      become: yes
      when: ansible_os_family == "Debian"

    - name: Install curl (required for nvm)
      ansible.builtin.apt:
        name: curl
        state: present
      become: yes
      when: ansible_os_family == "Debian"

    - name: Remove system Node.js to prevent nvm conflicts
      ansible.builtin.apt:
        name: nodejs
        state: absent
      become: yes
      when: ansible_os_family == "Debian"

    - name: Install btop (Debian) ⚙️
      ansible.builtin.apt:
        name: btop
        state: present
      become: yes
      when: ansible_os_family == "Debian"

    - name: Install fastfetch via GitHub (Debian) 🪄
      ansible.builtin.shell: |
        curl -sL https://github.com/fastfetch-cli/fastfetch/releases/latest/download/fastfetch-linux-amd64.deb -o /tmp/fastfetch.deb
        sudo dpkg -i /tmp/fastfetch.deb
        rm /tmp/fastfetch.deb
      args:
        creates: /usr/bin/fastfetch
      become: yes
      when: ansible_os_family == "Debian"

    - name: Install fzf (Debian) 🔎
      ansible.builtin.apt:
        name: fzf
        state: present
      become: yes
      when: ansible_os_family == "Debian"

    - name: Install git alias dependencies (Debian)
      ansible.builtin.apt:
        name: ["xclip", "gh"]
        state: present
      become: yes
      when: ansible_os_family == "Debian"

    # --- macOS ---

    - name: Install btop (macOS) ⚙️
      community.general.homebrew:
        name: btop
        state: present
      when: ansible_os_family == "Darwin"

    - name: Install fastfetch (macOS) 🪄
      community.general.homebrew:
        name: fastfetch
        state: present
      when: ansible_os_family == "Darwin"

    - name: Install fzf (macOS) 🔎
      community.general.homebrew:
        name: fzf
        state: present
      when: ansible_os_family == "Darwin"

    # --- Cross-platform ---

    - name: Install nvm
      ansible.builtin.shell: |
        curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
      args:
        creates: "{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm/nvm.sh"
      changed_when: false

    - name: Install npm using nvm
      ansible.builtin.shell: |
        export NVM_DIR="{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm"
        [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
        nvm install --lts
      args:
        creates: "{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm/versions/node"
      changed_when: false

    - name: Write git aliases file 🔗
      ansible.builtin.copy:
        dest: "{{ lookup('ansible.builtin.env', 'HOME') }}/.gitconfig-aliases"
        content: |
          [alias]
                  st              = status
                  co              = checkout
                  cot             = checkout test
                  cob             = checkout -b
                  del             = branch -D
                  rename          = branch -m
                  rhead           = reset HEAD~
                  plog            = log --pretty=format:'%h%x09%an%x09%ad%x09%s'
                  pretty-logs     = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --branches
                  fco             = "!git branch | fzf | xargs git checkout"
                  stash-select    = !git stash list | fzf | awk -F: "{print \\$1}" | xargs git stash pop
                  my-prs          = !gh pr list -A $(git config user.name) | fzf | awk '{print $1}' | xargs gh pr view --web
                  clc             = "!git log -1 --format='%H' | tr -d '\\n' | { command -v pbcopy >/dev/null && pbcopy || command -v xclip >/dev/null && xclip -selection clipboard || xsel --clipboard --input; }"

    - name: Include git aliases in global gitconfig 🔗
      ansible.builtin.shell: |
        git config --global --get-all include.path | grep -qF "$HOME/.gitconfig-aliases" || \
        git config --global --add include.path ~/.gitconfig-aliases
      changed_when: false
