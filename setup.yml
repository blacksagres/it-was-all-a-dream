- name: Hammerhead Debian 🖖
  hosts: localhost
  become: yes
  tasks:
    - name: Install btop ⚙️
      ansible.builtin.package:
        name: btop
        state: present

    - name: Install fastfetch 🪄
      ansible.builtin.package:
        name: fastfetch
        state: present

    - name: Install fzf 🔎
      ansible.builtin.package:
        name: fzf
        state: present

    - name: Install tmux 💻
      ansible.builtin.shell: |
        apt install tmux -y

    - name: Remove existing catpuccin folder, if any 🧨
      ansible.builtin.shell: |
        rm -rf ~/.config/tmux/plugins/catppuccin
      become: no

    - name: Remove existing .tmux folder, if any 🧨
      ansible.builtin.shell: |
        rm -rf ~/.tmux
      become: no

    - name: Oh my tmux! 🐈‍⬛
      ansible.builtin.shell: |
        git clone https://github.com/gpakosz/.tmux.git ~/.tmux
      become: no

    - name: Create symlink for .tmux.conf 🐈‍⬛
      ansible.builtin.shell: |
        ln -sf ~/.tmux/.tmux.conf ~/.tmux.conf
      become: no

    - name: Reload tmux configuration 🐈‍⬛
      ansible.builtin.shell: |
        tmux source-file ~/.tmux.conf
      become: no

    - name: Install nvm
      ansible.builtin.shell: |
        curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
      args:
        creates: "{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm/nvm.sh"

    - name: Install npm using nvm
      ansible.builtin.shell: |
        export NVM_DIR="{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm"
        [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
        nvm install --lts
      args:
        creates: "{{ lookup('ansible.builtin.env', 'HOME') }}/.nvm/versions/node"
